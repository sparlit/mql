interface:
  display_name: "Mql"
  short_description: "Repo-specific patterns and workflows for mql"
  default_prompt: "Use the mql repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true